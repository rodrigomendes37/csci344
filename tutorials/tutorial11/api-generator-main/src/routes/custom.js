function registerCustomRoutes(_app, _db) {
  /*
   * Add hand-written Express routes here when students are ready to move beyond
   * the generated CRUD API.
   *
   * Example:
   * _app.get("/api/hello", (_req, res) => {
   *   res.json({ message: "Hello from a custom Express route." });
   * });
   */
}

module.exports = {
  registerCustomRoutes,
};
